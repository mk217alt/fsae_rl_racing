# Third-party framework (cloned separately, not part of this project)
IsaacLab/

# Isaac Lab / Isaac Sim run artifacts
logs/
outputs/
__pycache__/
*.pyc

# Session run logs and ephemeral markers (regenerated by every run)
*.log
*_done*.txt
*_ready.txt
*_log*.txt
*_result*.txt
*_telemetry.txt
*_recovery_stats.txt
record_driving_progress.txt
*_action_deltas.csv

# checkpoint_backups/ (one model per training round) is published so that every
# evaluation in the thesis can be re-run; only this smoke-test checkpoint is excluded
checkpoint_backups/car_race_ppo_model_smoke_test_after_move.zip

# Superseded iteration checkpoints from the BC fine-tuning experiment
# (kept: the unsuffixed final ones - car2_bc_finetuned.zip etc.)
car2_bc_finetuned_v*.zip
car2_bc_pretrained_v*.zip
car2_bc_warmed_v*.zip

# Editor / tooling
.claude/

# Stray files not part of this project
2.7.0+cu128
testing.usd
thesis-backup.usd

# Thesis manuscript (kept local, not published with the code)
thesis_document/
